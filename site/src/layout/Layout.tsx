import { AppBar, Box, Toolbar } from "@mui/material";

export const Layout = () => {
  return (
    <Box>
      <AppBar position="static">
        <Toolbar></Toolbar>
      </AppBar>
    </Box>
  );
};
